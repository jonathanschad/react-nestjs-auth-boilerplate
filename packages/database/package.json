{
  "name": "@boilerplate/prisma",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "db:generate": "dotenv -e ../../.env -- prisma generate --no-engine && pnpm build",
    "db:migrate": "dotenv -e ../../.env -- prisma migrate dev && pnpm build",
    "build": "rm -rf dist && tsc && cp -r src/generated dist/"
  },
  "dependencies": {
    "@prisma/client": "6.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "copyfiles": "^2.4.1",
    "dotenv-cli": "^8.0.0",
    "prisma": "catalog:prisma",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
