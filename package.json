{
    "name": "react-nestjs-auth-boilerplate",
    "version": "0.5.0",
    "description": "",
    "scripts": {
        "dev": "pnpm generate:prisma && pnpm run --parallel dev",
        "build": "pnpm generate:prisma && pnpm run --parallel build",
        "lint": "pnpm run --parallel lint",
        "lint:fix": "pnpm run --parallel lint:fix",
        "postinstall": "pnpm generate:prisma",
        "generate:prisma": "pnpm --filter @boilerplate/prisma db:generate"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "packageManager": "pnpm@10.8.1",
    "engines": {
        "node": ">=22.4.0"
    }
}
